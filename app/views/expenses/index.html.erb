<%= render partial: 'shared/navbar' %>
<section class="container-fluid main-container">
<div class="container-fluid transract-card">
<h3>Total Amount: $<%= @total_expenses %></h3>
</div>
<div class="container transracts_container">
<% @expenses.each do |expense| %>
  <div class="container-fluid catergory-card">
  <%= image_tag "https://cdn-icons-png.flaticon.com/128/9080/9080379.png", size: 8*10 %>
  <li><%= expense.expense.name %> - <%= expense.expense.amount %></li>
  </div>
  <% end %>
  </div>
  <div class="fixed-bottom">
    <div class="btn_container">
    <%= link_to "Add new Transaction", new_category_expense_path, class:"btn_size" %>
    </div>
  </div>  
</section>